<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="autosave">
	<select id="trainerItem" resultType="UserVO">
		select user.username, point, (select count(um.username) from user_management um 
		where um.manager=user.username and reading="Y") menti
		from user
		where authority = trainer
	</select>
</mapper>