<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="reviewboard">
	<select id="list" resultType="ReviewBoard">
		select * from review_board
	</select>
	
	<select id="item" resultType="ReviewBoard">
		SELECT * FROM review_board WHERE review_id=#{review_id}
	</select>
	
	<insert id="add">
		INSERT INTO review_board(review_id, review_title, review_contents, review_date, review_photo, 
		review_views, review_score, id)
		VALUES (#{review_id}, #{review_title}, #{review_contents}, NOW(), null, #{review_views},
		null, #{id})
	</insert>
</mapper>