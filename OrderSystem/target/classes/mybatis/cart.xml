<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cart">
	<select id="amountSelect" resultType="Integer">
		select count(*) from bucket
		where menu_id = #{code}
	</select>

	<insert id="bucketAdd">
		insert into bucket (menu_id,id,amount)
		values
		(#{code},null,1)
	</insert>

	<update id="amountUpdate">
		update bucket
		set amount = amount +1
		where menu_id = #{code}

	</update>
	
	<select id="cartList" resultType="Bucket">
	select bucket.menu_id menu_id,menu_name,amount,menu_price from bucket, menu where bucket.menu_id = menu.menu_id
	</select>
	
	<delete id="cartDel">
	delete from bucket where menu_id = #{code}
	</delete>
</mapper>